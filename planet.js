// planet.js
class Planet {
    constructor(name, radius) {
        this.name = name;
        this.radius = radius;
        // Otras propiedades y métodos relacionados con el planeta
    }
}
